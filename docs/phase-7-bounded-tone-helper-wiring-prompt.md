# Phase 7 bounded-tone helper wiring execution prompt

At Qualification Harness revision
`1f410d88f0b3617ce1422ecf3ef909afcb897a4e`, expose WsprryPi's reviewed
`bounded_tone` WebSocket transaction through the authenticated capability
helper. Bind the literal-loopback endpoint and exact WsprryPi source revision
into both the resolved live plan and helper configuration. Preserve each
correlated helper transaction as immutable, explicitly non-qualifying evidence
and replace the Phase 7 per-cycle WsprryPi process launches with the helper
operation.

Fail closed on revision, digest, endpoint, response, deadline, cadence, or
cleanup contradictions. Add schema synchronization and deterministic
hardware-free tests for success and injected failures. Evidence or a plan
prepared for `f476dd2` or any earlier revision is stale and must never be
reused; every revision- or digest-bound plan, configuration, authorization,
and evidence artifact must be regenerated.

Do not deploy to either Raspberry Pi, access hosts, alter services, open an
SDR, touch GPIO/I2C, or emit RF. Run complete portable validation, correct all
adversarial findings, commit and push the attributable slice, and obtain green
macOS, Ubuntu, and native Windows CI.
